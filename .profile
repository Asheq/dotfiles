# ~/.profile is executed by the command interpreter for login shells. The login
# shell may be different on different systems so make sure everything in here
# is 100% POSIX-compliant.

# If the login shell happens to be bash, this file is not read by bash if
# ~/.bash_profile or ~/.bash_login exists.

. "$HOME"/.sh/helper_functions.sh
ssource ~/.sh/login.sh
