-- Optional built-in plugins in $VIMRUNTIME/pack/dist/opt/ must be loaded
-- explicitly with :packadd.
-- ============================================================================

-- Load cfilter
vim.cmd("packadd cfilter")
